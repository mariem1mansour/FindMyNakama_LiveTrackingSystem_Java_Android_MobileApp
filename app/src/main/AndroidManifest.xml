<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISSIONS: Ce sont les autorisations que l'app demande à l'utilisateur -->

    <!-- Permission pour utiliser Internet (nécessaire pour Firebase et Google Maps) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permission pour accéder à la localisation précise (GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Permission pour accéder à la localisation approximative (réseau) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Permission pour la localisation en arrière-plan (Android 10+) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Permission pour recevoir des notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FindMyNakama"
        tools:targetApi="31">

        <!-- Clé API Google Maps (À REMPLACER avec ta propre clé) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />

        <!-- Activité principale avec la carte -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.FindMyNakama" />

        <!-- Activité d'ajout d'amis -->
        <activity
            android:name=".AddFriendActivity"
            android:exported="false"
            android:label="Ajouter un ami"
            android:parentActivityName=".MainActivity" />

        <!-- Activité de liste d'amis -->
        <activity
            android:name=".FriendsListActivity"
            android:exported="false"
            android:label="Mes amis"
            android:parentActivityName=".MainActivity" />

        <!-- Activité de connexion (c'est l'écran de démarrage) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.FindMyNakama">
            <intent-filter>
                <!-- Cette activité se lance au démarrage de l'app -->
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activité d'inscription -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false"
            android:label="Inscription"
            android:parentActivityName=".LoginActivity" />

        <!-- Service pour la localisation en arrière-plan -->
        <service
            android:name=".LocationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>